Implement workflow-concurrency-global-lock in this order: (1) add `MAX_CONCURRENT_WORKFLOWS` and `WORKFLOW_LOCK_WAIT_SECONDS` configuration, plus a global `asyncio.Semaphore` in a new `src/service/workflow_lock.py`; (2) modify `src/runtime/routers/workflows_router.py::run_workflow_extension_endpoint` to acquire the semaphore with timeout and return HTTP 503 + `Retry-After` on timeout; (3) harden `ExtensionManager` global state (`_connections`, `_step_futures`) in `src/runtime/websocket_manager.py` and `ExtensionRunner._active_runners` in `src/runtime/workflow/extension_runner.py` with `asyncio.Lock`; (4) run `npm run harness:check`; (5) smoke-test by starting uvicorn, verifying a single extension run still works, and firing a second concurrent request to confirm it receives 503 or queues cleanly; (6) update `.harness/feature_list.json` and append `.harness/PROGRESS.md`.
