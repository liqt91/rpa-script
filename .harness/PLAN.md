Implement not-found-page in this order: (1) add a FastAPI exception handler in `src/runtime/main.py` that catches `StarletteHTTPException` with status 404 and returns a custom HTML body (or JSON for API clients) at any unmatched path; (2) run the structural test with `npm run harness:check`; and (3) smoke-test by starting the dev server and curling `/no-such-path` to verify HTTP 404 with the custom body.
