Implement health-endpoint in this order: (1) add `HealthResponse` Pydantic schema in `src/dtypes/schemas.py` with field `status: str`; (2) add `src/service/health_service.py` exposing `get_health()` returning `{"status": "ok"}`; (3) add a health router in `src/runtime/routers/other_routers.py` with `GET /api/health` returning the service result; (4) register the health router in `src/runtime/main.py` before the SPA routes; (5) run the structural test with `python -m harness.check`; and (6) smoke-test by starting the dev server and curling `GET /health` to verify 200 + `{status:'ok'}`.
