Implement ADR 0006 in this order: (1) add `element_kind` to `WorkflowElement` and an Alembic migration that maps legacy `anchor_mode` values; (2) update `WorkflowElementIn/Out` schemas; (3) enforce save-time validation in `elements_service.py`; (4) expose `elementKind` in the extension element library route; (5) refactor the extension sidepanel and content script so child capture is anchor-first, relative selectors auto-recompute on candidate/family changes, and manual edits set `anchor_mode='manual'`; (6) route runtime/exporter resolution by `element_kind`; and (7) add an end-to-end pytest covering child capture, persistence, and runtime resolution. Run `python -m harness.check` and `ruff` after each backend step.
