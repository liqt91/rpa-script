# 工作日志 2026-07-30

## 一、WebRPA 调研

### 1. 项目概况

调研了 [pmh1314520/WebRPA](https://github.com/pmh1314520/WebRPA)（1821 star），一款成熟的零代码 RPA 工具。

**技术栈：**
- 后端：FastAPI + Playwright（浏览器自动化）+ uiautomation（桌面）
- 前端：React + TypeScript + Vite + @xyflow/react（流程图）+ Radix UI + Tailwind
- 启动器：Electron + Vue 3（打包成单 exe，系统托盘/开机自启）
- 集群：orchestrator（节点注册/心跳/认领任务/上报结果）
- AI：Socket.IO 流式对话 + 技能注册表

**规模：** 260 个执行器、46 个 API 模块、70 个前端组件。

### 2. 与我们的架构对比

| 维度 | WebRPA | 我们 |
|------|--------|------|
| 浏览器 | Playwright CDP | Chrome Extension |
| 元素拾取 | JS 注入 + CSS 选择器 | Extension content script |
| 元素存储 | 无元素库（每次捕获） | WorkflowElement 元素库 |
| 列表循环 | nth-child 通配符 | anchor/child 标注 + 运行时相对定位 |
| 桌面拾取 | UIA 轮询 | Win32 XOR + UIA 双通道 |
| 已登录浏览器 | ❌ 无法复用（CDP 需重启） | ✅ 扩展直接控 |
| 指令架构 | 执行器大文件 | handler 模式（一指令一文件） |
| 前端 | React 成熟 | tkinter demo + React 半成品 |

### 3. 值得借鉴

- **@xyflow/react** 流程图编辑器（可替代手写画布）
- **orchestrator API 设计**（集群节点管理）
- **AI 助手** Socket.IO 流式交互
- **拾取体验** 蓝色高亮 + 绿色闪烁确认
- **桌面宏录制**（pynput 监听 → 自动生成步骤）

### 4. 不需要学的

- **Playwright** 替代 Chrome Extension（我们的扩展方案更强）
- **执行器大文件**（我们的 handler 模式更好）
- 他的元素拾取**成功率低**（百度搜索 3 次只成功 1 次，层级不一致、卡片被遮挡）

## 二、GUI 编辑器改进

### 1. 属性编辑器修复

- **修复** `_clear_properties` 只清 grid 不清 pack 的问题（占位文字不消失）
- **修复** select 类型字段保存 label 而非 value（"Chrome" → "chrome"）
- **修复** 旧数据格式 `{"extra": {...}}` 双层嵌套导致运行时读不到参数
- **修复** `auto_register()` 未调用导致新体系指令缺失
- 添加保存失败弹窗 + debug 日志

### 2. 拖拽优化

- 新增 ghost 窗口跟随光标（半透明蓝色标签）
- 目标行高亮改用蓝色背景（代替文本插入线）
- 死区从 5px 降到 3px
- 支持命令面板拖入 + 步骤列表内排序

### 3. Native Messaging 改造（GUI 侧）

- 新建 `scripts/native_host.py`（Chrome 拉起的中继进程，stdin/stdout ↔ WS）
- 新建 `scripts/register_native_host.py`（写注册表 + 自动推导扩展 ID）
- `editor.py` 启动时自动注册 Native Host

## 三、content_capture.js 拾取体验优化

- 高亮颜色：红色 → 蓝色（#3b82f6），边框 2px → 3px
- Toast 提示：右下角黑底 → 顶部居中（info/success/error 三色）
- 新增**捕获成功绿色闪烁动画**（0.3s 放大淡出）

## 四、元素捕获改进方案（待实现）

详见: [GUI 统一捕获方案](./GUI统一捕获方案.md)

### 要点

| 捕获方式 | 说明 |
|----------|------|
| 桌面控件 | GUI 直接 Win32+UIA, HWND rect 高亮, UIA 细粒度定位 |
| 浏览器 DOM | GUI → WS → Extension content script → CSS/XPath 选择器 |

### 已完成 (GUI)

| 特性 | 说明 |
|------|------|
| 边框高亮 | SetWindowRgn 挖空窗口, 3px 蓝色边框, WS_EX_TOPMOST 永远在最上面 |
| UIA 细粒度 | 桌面图标/菜单项/列表项精确到 UIA ListItemControl |
| 方向键导航 | ↑ 选父级, ↓ 退子级, Enter/左键确认 |
| 信息浮窗 | 左上/右上 自动避让, 显示 6 层祖先链 + 尺寸 |
| 验证闪烁 | 空心边框 绿色/红色 (不再全屏色块) |
| DWM 去阴影 | DwmGetWindowAttribute 取真实可见边界 |

## 五、改动的文件清单

| 文件 | 操作 |
|------|------|
| `extension/content_capture.js` | 修改：蓝色高亮、绿色闪烁、toast 样式 |
| `scripts/desktop_editor/editor.py` | 修改：属性编辑器修复、拖拽优化、auto_register、native host |
| `scripts/native_host.py` | 新建 |
| `scripts/register_native_host.py` | 新建 |
| `src/runtime/workflow/extension_emitter.py` | 修改：_parse_extra 兼容旧数据格式 |
| `src/runtime/commands/extension_commands/launchBrowser.py` | 修改：debug 日志 |
| `docs/工作日志-20260730.md` | 新建 |


---

# 工作日志 2026-08-04

## 一、GUI 统一捕获 — 浏览器元素捕获链路打通

### 问题
- 桌面元素捕获已有（overlay.py），但浏览器 DOM 元素捕获链路一直不工作
- 扩展的 content_capture.js 已有成熟选择器生成，需复用

### 最终方案
GUI HTTP POST → FastAPI ext_manager → Extension WS → content_capture.js → 用户点击 → WS回传

### 关键发现
1. 扩展实际加载 dist/desktop/extension/ 目录（非 extension/），一直改错文件
2. __rpaCaptureInjected guard 阻止了第二次注入新脚本
3. ensureContentScripts 需要在 sendMessage 前调用
4. websockets>=13 默认拒绝所有 origin，需 origins=None

### 新增端点
- POST /api/extension/gui-browser-capture
- POST /api/extension/verify-selector

## 二、GUI Web 元素编辑器重写

### 推荐方案面板
- 只保留 CSS/XPath（过滤 drission）
- Treeview 三列可排序: 选择器 | 类型 | 匹配
- 选中候选同步到选择器框

### 手动编辑面板（DOM 层级）
- 树形缩进 + 每层复选框 → 勾选层级生成选择器
- 每层元素属性可单独开关
- 全选/全不选
- 叶子节点红色加粗高亮

### 验证
- Web 元素: HTTP → content_script → querySelectorAll / document.evaluate → 页面蓝色outline闪烁
- 桌面元素: HWND闪烁（不变）

### 保存选择器
- 点击保存 → 写入 css_selector → 重选元素时默认显示已保存值
- _populating flag 解决 selection_set 事件覆盖问题

### 布局
- 三段式: 名称行 → 中间撑满 → 底部选择器+按钮
- Tab 按钮 ▶ 前缀标记活跃态
- 截图缩略图在名称行右侧，点击放大

## 三、调研
- 影刀: 同我们方案，GUI桌面 + Extension浏览器双通道
- Chrome DevTools MCP: 需CDP端口，不适用
- DrissionPage: 需CDP端口，同限制

## 四、涉及文件
| 文件 | 变更 |
|------|------|
| scripts/capture_gui/main.py | 完全重写(440行→336行精简) |
| scripts/capture_gui/overlay.py | ElementInfo加candidates/screenshot/dom_path/attrs/list_info |
| scripts/capture_gui/ws_client.py | WS→HTTP |
| scripts/capture_gui/store.py | 新字段持久化 |
| src/runtime/routers/extension_router.py | gui-browser-capture + verify-selector |
| extension/content_capture.js | launchBrowserCapture + verifySelector + 视觉闪烁 |
| extension/background_base.js | launchBrowserCapture + verifySelector + ensureContentScripts
---

# 2026-08-05 工作日志

## 一、Web 元素捕获工具（模态框）

### 1. 前端重写
- 新建 `CaptureToolModal.jsx`（原生 React 模态框，替代 iframe）
- **布局参考影刀**：移除元素列表区域，捕获后直接编辑当前元素
- 流程：🔍捕获元素 → GUI 浮窗拾取 → 直接填入编辑器 → 验证/保存
- 推荐方案/手动编辑 tab 切换，手动编辑分"页面层级+元素属性"两栏
- 候选表格可排序、可滚动（表头 sticky）
- 选择器下方显示控制台验证 JS 代码（CSS/XPath 自动生成，可复制）

### 2. 桌面化
- 新建 `desktop/` Electron 外壳（main.js + package.json）
- 自动启动 Python 后端（uvicorn 8000）+ 打开编辑器窗口
- 端口占用检测：已运行则直接复用
- Electron 31 安装（npmmirror 镜像，GitHub 直连失败）

## 二、GUI 捕获链路

### 1. 一次性捕获入口
- 新建 `scripts/capture_gui/capture_once.py`（子进程调用 run_capture → JSON stdout）
- 端点 `POST /api/commands/gui-picker`（subprocess 60s 超时）
- 修复：libpng C 级警告污染 stderr → capture_output 优先解析 stdout；stdout UTF-8 重配（GBK 编码 \u200b 失败）

### 2. 桌面验证
- 新建 `scripts/capture_gui/verify_once.py`（stdin 传 ElementInfo → flash_element 闪烁）
- 端点 `POST /api/commands/gui-verify`

### 3. 名称/查重
- 捕获名称优先 data.name（web 元素避免叫"web"）
- 同名自动追加 _2 _3 后缀（UNIQUE 约束）

## 三、Web 元素验证

### 1. 验证链路
- 模态框 → `/api/extension/verify-selector` → 扩展 background → content_capture.js verifySelector
- **只查可见页面**（聚焦窗口活动页），找不到立即返回
- 移除 tabId 方案（会变，无意义）
- 过滤编辑器页(127.0.0.1:8000)/扩展页/chrome://页

### 2. 框选修复
- **根因1**：ensureContentScripts 反复注入 → 多个监听器叠加（旧 1500ms 框+新闪烁）→ 监听器防重复注册
- **根因2**：el.style.outline 在容器元素上不可见 → 复用侧边栏 highlightSelectorMatches 机制（固定 div 边框+半透明底）
- **根因3**：可见性过滤（resolveAllForVerify 的 isRenderedVisible）
- 闪烁 3 次后消失（180ms 亮 + 120ms 灭）

### 3. 选择器前缀
- 扩展候选 syntax 带 `css:`/`xpath:`/`verse:` 前缀 → 存库/显示/验证统一 strip

## 四、涉及文件
| 文件 | 变更 |
|------|------|
| src/ui/workflow-editor/src/components/CaptureToolModal.jsx | 重写(移除元素列表, 影刀布局) |
| desktop/main.js + package.json | 新建 Electron 外壳 |
| scripts/capture_gui/capture_once.py | 新建(子进程捕获) |
| scripts/capture_gui/verify_once.py | 新建(子进程验证) |
| scripts/capture_gui/overlay.py | 选择器去前缀; 移除tab_id |
| src/runtime/routers/commands_router.py | gui-picker + gui-verify 端点 |
| src/runtime/routers/extension_router.py | verify-selector 只查可见页 |
| extension/content_capture.js | verifySelector 闪烁3次+div高亮; 监听器防重复 |
| extension/background_base.js | verifySelector 可见页定向 |
