# 工作日志 2026-07-28

## 一、AI 配置与指令代码生成

### 1. AI 配置模块

- 新增 `AILLMConfig` 数据模型（`ai_llm_configs` 表），支持 DeepSeek 配置、模型选择、场景 Prompt 管理
- 新增 API：
  - `GET /api/ai/llm-config`
  - `PUT /api/ai/llm-config`
  - `POST /api/ai/llm-config/scenarios/{scenario_id}/generate`
- 前端新增「AI 配置」侧边栏页面，支持服务商、模型、API Key、场景 Prompt 编辑和测试生成
- 默认模型改为 `deepseek-v4-flash`，新增 `deepseek-v4-pro` 可选
- 数据库 migration 012 自动添加 `model` 列

### 2. AI 生成 handler 代码

- 重写默认 Prompt，给出项目实际的 `from ..registry import register_handler, Param` 完整模板
- 后端注入 `{{type}}`、`{{label}}`、`{{category}}`、`{{ClassName}}` 变量
- 自动清洗 LLM 返回的 Markdown 代码块
- 生成代码语法校验（`compile()`）
- 后端控制台打印原始响应和提取后的代码，便于调试
- 新增场景：`command_backend`、`command_extension_js`、`command_control`、`command_review`、`test_flow_gen`

### 3. CommandEditor 界面

- 从 4 列改为 5 列：指令配置 | JSON 预览 | Python Handler | JS Handler | 测试
- Python/JS 代码支持直接编辑和保存
- AI 生成/审查按钮集成到对应标签页

## 二、新指令体系一致性检查

- 确认 52 个指令全部使用新架构（`cmd` 字段 + `isNew: true`）
- 修复 `setVar.json` 的 `handler.source` 旧路径：`handlers/backend/set_var_impl.py` → `src/runtime/commands/backend_commands/setVar.py`
- 21 个指令补齐 `"isNew": true`
- `generate_commands.py` + `build_content_js.py` 验证通过

## 三、指令测试体系

### 1. 单指令沙盒（快速预览）

- 新增 `POST /api/commands/definitions/{type}/test`
- 支持 backend 指令直接执行、extension 指令通过浏览器真实执行
- 前端测试列支持参数表单、窗口选择、结果展示

### 2. 上下文感知

- 新增 `GET /api/commands/sandbox/context`：返回当前已连接的浏览器窗口列表
- 测试时可选择真实浏览器窗口，`_send_and_wait` 真实发送到浏览器

### 3. 测试流程模板 + AI 生成

- 指令 JSON 新增 `testTemplates` 字段，定义最小可运行测试流程
- 新增 API：
  - `GET /api/commands/definitions/{type}/test-templates`
  - `POST /api/commands/definitions/{type}/test-flow`
  - `POST /api/commands/definitions/{type}/generate-test-flow`
- 前端测试列改为可收起，支持「快速预览」和「测试流程」两种模式
- 测试流程模式支持模板选择、AI 生成、节点编辑、流程执行
- 示例模板：`clickElement`（百度搜索按钮点击）、`setVar`（设置数字变量）

## 四、测试修复与兼容性

- `test_ai.py`：默认场景断言更新为新的 5 个场景
- `test_capture_element_kind.py` / `test_legacy_workflow.py` / `test_workflows.py`：`WorkflowNode(type=...)` 改为 `cmd=...`，`click` 改 `clickElement`，`endFor` 改 `endLoop`
- `test_handler_backend.py`：async 测试补 `@pytest.mark.asyncio`
- `test_workflows.py`：`@pytest.mark.anyio` 改为 `@pytest.mark.asyncio`
- `handlers/utils.py`：`resolve_vars` / `resolve_vars_json` 同时支持 `{{var}}` 和 `${var}` 两种变量语法
- `convert_value` 对 `any-input` 类型增加自动推断：先尝试 JSON 解析，失败退回字符串
- 新增 `test_command_sandbox.py`：6 个测试覆盖单指令沙盒和测试流程 API

## 五、其他

- 4 个 RPA skill 从 `.deepcode/skills/` 移动到 `skills/`，不再绑定 Deep Code
- 导出 `docs/ai-command-generation-requirements.md` 需求文档

## 六、验证结果

- `python scripts/generate_commands.py`：52 个指令处理成功
- `python scripts/build_content_js.py`：构建成功
- `npm run build`：前端构建通过
- `npm run harness:check`：结构测试通过
- `pytest src/runtime/tests/`：**127 passed**
