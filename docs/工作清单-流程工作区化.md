# RPA 流程工作区化 —— 工作清单（存档）

> 目标：一个 RPA 流程 = 一个目录 = 一个 DSH 工作区；流程数据（workflow.json/
> elements.json/data.json/images/run_logs）存于目录内；编辑免后端、运行轻量化。
> 存档时间：2026-08-18。状态更新见各条目。

## 阶段 0：收尾当前遗留
- [x] 0.1 `rpa_project_create` 去掉 `withEnsure` —— 初始化不再依赖/拉起 Python 后端（node fs + workspaceRegistry 即可）
- [x] 0.2 验证拖拽创建节点 + 无窗口重启后端（已修复：saveNode wfId 拦截、windowsHide + argv 无 shell 启动）

## 阶段 1：编辑免后端（核心）
- [x] 1.1 新增 `/rpa-bridge/project/read`、`/rpa-bridge/project/write`（node 侧 fs）—— 白名单文件名 + rpa.json 校验 + 原子写；CORS 跨源支持
- [x] 1.2 前端 `api.js` 项目读写按 dshBase 指向 `/rpa-bridge/*`（编辑免后端）；无 dshBase 回退 `/api/projects/*`
- [x] 1.3 元素库/数据表格迁入目录文件 —— DataTableTab 支持 projectDir（存目录 data.json）；元素随 workflow.json 读写
- [x] 1.4 移除 AIConfigPage / ApiSettingsPanel / invokeAI / LLM 配置入口 —— AI 已集成在 dsh
- [x] 1.5 页面托管迁到 dsh web（/rpa-editor/ 静态路由）—— 后端 kill 后页面仍可打开（含 vite base 相对化、prefix 尾斜杠、路径分隔符规范化、静态文件同步两处插件目录等系列修复）
- [x] 1.6 移除旧指令体系（/commands）—— 只保留 commands-new（见阶段 3）

## 阶段 2：运行时目录化（2.4 搁置）
- [ ] 2.1 `extension_runner` 加"目录数据源"分支 —— 从 workflow.json/elements.json 加载运行，绕过 SQLite（现在只吃 DB 对象）
- [ ] 2.2 `rpa_run_start` 支持按项目目录运行 —— 传 project 目录替代 wf_id（或两者兼容）
- [ ] 2.3 运行日志写回目录 `run_logs/` —— 现在全局 data/run_logs
- [ ] ~~2.4 评估轻量 WS 宿主（纯 websockets 脚本）~~ —— 用户搁置（2026-08-18）：先跑通目录化，再看是否值得

## 阶段 3：指令目录静态化
- [x] 3.1 构建时导出指令目录 JSON 进前端包 —— `scripts/export_commands.py` 只导出 commands-new.json（69 条）到 public/；dsh 托管模式前端读静态 JSON，独立模式回退后端 API。指令更新需重跑导出脚本
- [x] 3.2 移除旧指令体系（/commands 的 DB + handler 注册表加载）—— WorkflowContext/CommandPanel/api.js 全部只用 newCommands；删 CommandsPage.jsx、api.getCommands、commands.json 产物

## 阶段 4：扩展适配（4.2 搁置）
- [ ] 4.1 扩展运行参数支持 project 目录 —— 目前只传 workflow_id
- [ ] ~~4.2 桌面版（desktop/main.js）同步目录化~~ —— 用户搁置（2026-08-18）：桌面形态暂不维护

## 决策记录
- 数据真相：目录为唯一真相（workflow.json/elements.json/data.json 存目录，SQLite 只留全局共享）
- 编辑链路：B 类（纯文件读写）迁到 dsh 插件 node 侧（/rpa-bridge/* + /rpa-editor/ 静态托管），不依赖 Python 后端
- 运行链路：Python 引擎保留（extension_runner），需加目录数据源；web 服务形态待定
- AI：编辑器内 AI 功能移除（已集成在 dsh）
- 指令体系：旧 /commands（DB + handler 注册表）已移除，只用 commands/*.json 新指令体系
- 初始化：不依赖后端（node 侧 mkdir + rpa.json + workspaceRegistry）
