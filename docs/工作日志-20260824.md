# 工作日志 2026-08-24

## 一、elements.json 拆分落地（捕获写回与编辑器写入域分离）

### 背景

08-19 完成的统一捕获 P0 把捕获元素写进了 `workflow.json` 的 `elements[]`——但编辑器对
workflow.json 是整文档读-改-写（`WorkflowContext.commit`），捕获写进去的元素会被编辑器
下次保存用旧内存副本覆盖。按 `docs/capture-unification-plan.md` v2.2 协议拆分到
`elements.json`（用户已拍板）。

### 改动

**写侧（捕获/保存 → 只写 elements.json，不碰 workflow.json）**

- `scripts/capture_gui/store.py` `save_element_to_workspace`：
  落点改 `elements.json`（`{version:1, elements:[...]}`，同名替换，tmp+replace 原子写）；
  不再创建/修改 workflow.json。
- `src/runtime/routers/project_router.py`：
  新增 `_load_project_elements`（elements.json 优先 + workflow.json 遗留 elements[]
  按名合并兜底，不强制迁移）与 `_save_project_elements`（原子写 elements.json）；
  `project_save_element` / `project_register_image` 两端点切到新落点。
  `_load_workflow_data` 保留（供遗留合并）。

**读侧（合并读取）**

- `src/runtime/workflow/extension_runner.py` `load_project_workflow`：
  elements.json 优先 + workflow.json 遗留 elements[] 按名补齐 → element_map。
- `src/ui/workflow-editor/src/store/WorkflowContext.jsx`：
  新增 `readProjectElements`（同样的合并语义）；
  `loadWorkflow` / `loadElements` 项目模式改走合并读取。

**编辑器 commit（双写分离 + 惰性迁移）**

- workflow.json payload 不再携带 `elements`（只写 name/description/url/parameters/nodes）。
- 元素单独写 elements.json：**先**读最新盘（可能刚有捕获写入）→ 按名合并编辑器内存
  （磁盘优先，编辑器只补缺名）→ 原子写 elements.json，**再**写 workflow.json——
  中途失败时 workflow.json 的遗留元素仍在，可重试，无数据丢失窗口。
- 首次 commit 自动把 workflow.json 遗留元素搬进 elements.json（惰性迁移）。

**文案**

- `rpa-dsh-plugin/lib/index.js`：rpa_capture 工具描述 / /rpa_capture 命令描述与成功
  提示的 "workflow.json 元素库" → "elements.json 元素库"。

### 测试

- 新增 `src/runtime/tests/test_elements_json_split.py`（7 条）：
  写 elements.json 不碰 workflow.json / 同名替换 / 非工作区报错 /
  elements.json 优先级合并 / 缺文件回退遗留 / runner 合并 / runner 无 elements.json。
  **7/7 通过**。
- 回归 `test_capture_element_kind.py` + `test_capture_desktop_hierarchy.py`：**15/15 通过**。
- `npm run harness:check`：**PASSED**。
- 前端 `npm run build` → bundle `index-DJs2zAd2.js`，已 /MIR 同步到
  `profiles/web/rpa-dsh-plugin` 与 `profiles/web/node_modules/rpa-dsh-plugin` 两处
  （含清理残留旧 bundle CrY5UVxl/DXJPYjMy；index.html 均引用新 bundle）。

### 兼容性

- 存量只有 workflow.json 内嵌元素的项目：读取三处（编辑器/运行器/后端端点）全部
  遗留合并兜底，不丢元素；首次编辑器保存后自动迁入 elements.json。
- elements.json 本就在 rpa-bridge / project_router 读写白名单内，无需改白名单。

### 待办（需用户配合）

- 重启 dsh web（插件 lib/index.js 为启动时加载）+ 后端（project_router 改动生效）。
- 实测：捕获 → elements.json 落盘 → 编辑器元素 tab 可见 → 编辑器改流程保存 →
  捕获元素仍在（竞态消除验证）→ 流程运行按选择器定位。

## 二、项目模式元素库 CRUD（拆分闭环最后一块）

### 背景

elements.json 拆分后暴露一个功能缺口：项目模式下元素库的「重命名 / 删除」按钮被
整体隐藏（`ElementLibraryTab` 里 `{!projectDir && ...}`，原走 DB 模式 API——项目模式
元素无 id，作者直接藏了按钮），导致捕获到的元素改名/删除/改选择器都做不了。

### 改动

- **`src/runtime/routers/project_router.py`** 新增两个按 name 定位的端点：
  - `PUT /api/projects/elements/update` —— `{name: 原元素名, updates: {...}}`，
    改选择器/重命名/改锚点；新名与既有元素冲突则按名合并去重。
  - `DELETE /api/projects/elements/delete` —— `?name=` 删除。
  均 `_load_project_elements` 合并读 + `_save_project_elements` 原子写回 elements.json。
- **`src/ui/workflow-editor/src/api.js`**：`projectUpdateElement` / `projectDeleteElement`。
- **`src/ui/workflow-editor/src/store/WorkflowContext.jsx`**：`readProjectElements` 给每个
  元素补稳定 `id`（`el:<name>`）供选中高亮/树节点/行内 CRUD 定位；commit 写盘前剥离
  `id`，只落业务字段。
- **`ElementLibraryTab.jsx`**：
  - 放开 `!projectDir` 限制，行内/详情区重命名、删除在项目模式走新端点（按 name）。
  - `updateAnchor` 项目模式也走新端点。
  - Web 元素详情区新增「编辑选择器」入口（项目模式专用，保存走 `project_update_element`）。

### 测试

- `test_elements_json_split.py` 新增加 `TestProjectElementCrudEndpoints`（6 条）：
  update_selector / rename / rename_collides_merges / delete / update_missing / delete_missing。
  **13/13 通过**。
  - 注：TestClient 需 `base_url="http://localhost"`——`host_guard` 中间件只放行本机
    host，默认 `testserver` 被拒（这是仓库既有 `test_workflows.py` 8 个 error 的同一根因，
    非本次引入）。
- 回归：`test_capture_element_kind` + `test_capture_desktop_hierarchy` 全过；
  `test_workflows.py` 8 error 为存量 Host 白名单问题，与本次无关。
- `npm run harness:check`：**PASSED**。
- 前端 `npm run build` → 同步 bundle `index-4C9DPgpb.js` 到两处 profile（含清理残留）。

### 待办（需用户配合）

- 重启 dsh web + 后端后，项目模式实测：选中元素高亮正常、行内/详情区重命名、删除、
  Web 元素改选择器、相对锚点设置——全部写回 elements.json 且编辑器/运行器可见。
