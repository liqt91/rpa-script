# 指令全量测试结果（第一轮：T2 浏览器元素操作）

日期：2026-08-14
测试方案：`docs/指令全量测试方案-20260814.md`
执行方式：`command-test` skill（build_workflow / run_workflow / read_run）
浏览器：Edge（扩展在线，dev 后端 :8000）｜目标：本地测试页 `http://127.0.0.1:8899/index.html`

## 1. 总览

| 工作流 | 名称 | 步骤 | 结果 | 耗时 |
|---|---|---|---|---|
| wf=34 | 元素操作指令测试-本地页 | 26（含 5 嵌套） | **24/24 完成，0 失败**（首跑 2 步异常=测试页缺陷，重跑全绿） | 10.2s |
| wf=35 | 元素操作诊断-本地页 | 10 | 10/10 完成，0 失败 | 8.2s |
| wf=36 | 元素操作补充-输入路径 | 11 | **11/11 完成，0 失败** | 7.6s |

## 2. 分步判定（wf=34 重跑，26 步全绿）

| # | 指令 | 结果摘要 | 判定 |
|---|---|---|---|
| 1 | launchBrowser | windowId/tabId 返回，Edge 打开本地页 | ✅ |
| 2 | waitForElement | appeared=true, count=1 | ✅ |
| 3 | clickElement | clicked=true, osClick=true, 校准后精确坐标 | ✅ |
| 4 | inputElement | osType=RPA自动化测试（OS SendInput 键入） | ✅ |
| 5 | pressKey | osKey=Enter（OS 层按键） | ✅ |
| 6 | getText | "输入:RPA自动化测试 回车!" —— 输入事件 + Enter keydown 均真实触发 | ✅ |
| 7 | getElementLink | https://www.baidu.com（首个链接，matchedCount=2） | ✅ |
| 8 | hover | 坐标 (587,326)，鼠标探针确认精确落点 | ✅ |
| 9 | getText | "悬停已触发" —— mouseover 真实触发 | ✅ |
| 10 | scrollIntoView | scrolled=true | ✅ |
| 11-13 | ifElementExists | exists-ok（真分支） | ✅ |
| 14-16 | ifElementVisible | visible-ok（真分支） | ✅ |
| 17-19 | ifUrlContains | url-ok（真分支） | ✅ |
| 20-22 | ifTextEquals | text-ok（真分支，compareTo 参数正常） | ✅ |
| 23-25 | forEachElement | 4 项遍历：0:项目0 … 3:项目3 | ✅ |
| 26 | takeScreenshot | 有效 PNG dataUrl（非空） | ✅ |

## 3. 预登记疑似 bug 验证（方案 §5.3）

| 编号 | 结论 | 说明 |
|---|---|---|
| W1 inputElement pressEnter | ✅ 通过（补充 wf=36） | 合成路径 pressEnter（合成 keydown 触发页面 Enter 监听）+ OS 路径 pressEnter（runner 发 OS Enter）均验证；**simulateKeyboard=false 合成事件路径、clearFirst 清空**一并验证 |
| W2 pressKey | ✅ 通过 | OS Enter 真实触发页面 keydown（"回车!" 出现） |
| W3 takeScreenshot | ✅ 通过 | 生成有效 PNG base64 |
| W4 getElementLink | ✅ 通过 | 返回首个匹配链接 href |
| W5 ifTextEquals | ✅ 通过 | compareTo 正常，真分支命中 |
| W6 hover | ✅ 通过 | OS 鼠标精确落点（探针显示 view/screen 一致）+ mouseover 触发 |

## 4b. 补充覆盖（wf=36，输入路径 4 项）

| # | 场景 | 结果 | 判定 |
|---|---|---|---|
| 4-5 | inputElement simulateKeyboard=false 合成输入 | span="输入:合成路径"（input 事件触发） | ✅ |
| 6-7 | 合成路径 + pressEnter=true | span="输入:回车测试 回车!"（合成 keydown 触发页面 Enter 监听） | ✅ |
| 8-9 | OS 路径（simulateKeyboard=true）+ pressEnter=true | span="输入:OS路径 回车!"（SendInput 键入 + OS Enter） | ✅ |
| 10-11 | clearFirst=true 清空后输入 | span="输入:清空测试"（若未清空应为"OS路径清空测试"） | ✅ |

## 4. 测试过程问题（非产品缺陷）

| 编号 | 类型 | 说明 |
|---|---|---|
| T1 | 测试页缺陷 | 首版测试页仅按钮点击触发搜索，无 Enter/input 监听 → wf34 首跑 step_6 取到空 span（0x0 → getText matchedCount=0）；重建探针页后全绿。**非产品 bug** |
| T2 | 测试页缺陷 | 首版页 hover 区域随页面内容变化导致首跑坐标略偏；合并版页（探针固定）后悬停触发正常 |
| T3 | 未覆盖 | inputElement `simulateKeyboard=false`（合成事件路径）、`pressEnter=true` 内建回车、inputElement `osClear` 清空行为 —— 留待补充 |

## 5. 涉及资产

- 测试页：`tmp/testpage/index.html`（本地静态服务 :8899，后台运行中）
- 工作流：wf=34（元素操作全量）、wf=35（诊断）—— `data/data.db`
- 元素库：wf=34 新建 8 个元素（测试标题/搜索框/搜索按钮/结果文本/悬停/链接/滚动/列表项，css 选择器）

## 6. 剩余待测（见方案）

- T3 桌面 Win32(12) + UIA(4)：真机手动
- 回归：变量及日志 / 循环 / 浏览器操作（既有方案）

> T2 浏览器元素操作已全覆盖（含合成事件/内建回车/清空路径），无待测项。

## 7. T1 纯后端 pytest（本轮新增，17 用例全过）

| 文件 | 用例 | 覆盖 | 结果 |
|---|---|---|---|
| `test_control_flow.py` | 15 | 条件判断 4（ifVarEquals 真/假/gt、ifListContains、ifDictContains、ifVarContains）；try/catch（成功/捕获/errorVar 注入）；循环边界（负步长、step=0 修正、空列表、while 熔断、break 立即退出、continue 全迭代） | ✅ 15/15 |
| `test_desktop_params.py` | 2 | 16 个桌面指令（12 Win32 + 4 UIA）注册为本地后端执行 + 参数 schema 完整/类型合法 | ✅ 2/2 |

- 走真实 emitter→runner 管线（无浏览器），沿用 command-test 的 DB 直写模式。
- 调试记录：复合指令（forRange/if*）的 stepComplete 结果**不进 `runner.results`**（仅 run.log/SSE），
  断言改用日志计数；log 条目为裸 `{"log","level"}`（P2 已知行为）。
- 全量 pytest 130 → **147 通过**；`harness:check` PASSED。

## 8. T3 桌面指令自动化（真机，目标记事本，8 过 1 xfail）

| 文件 | 结果 | 覆盖 |
|---|---|---|
| `test_desktop_commands.py` | **8 通过 + 1 xfail** | openAppWin32 / findWindowWin32(模糊) / findChildWin32(Edit) / inputControlWin32 / sendKeyWin32(Ctrl+A) / waitWin32 / closeWindowWin32 / findWindowUia 通过；clickMenuWin32 查找+点击成功 |

### 真机发现（记录不改码）

| 编号 | 位置 | 说明 | 状态 |
|---|---|---|---|
| **D3** | clickMenuWin32 | 参数提示写「文件→另存为」（U+2192），代码按 ASCII `->` 分割 —— 照提示填会「未找到菜单」 | ✅ **已修**（兼容 `→`/`->` 两种分隔符） |
| **D5** | pickElementUia | uiautomation `.Control(ClassName/AutomationId/Name, Depth)` 条件搜索对 **Win11 记事本子控件恒 exists=False**（直接 `GetChildren()` 可见但搜索不中）→ `pick_from_path` 子层级解析失败 | ✅ **已修**（`find_child_by_enum` 枚举兜底：GetChildren 直接匹配） |
| **D6** | clickMenuWin32 | 点击「退出」后窗口未关闭 → **非缺陷**：实为「是否保存」确认框（用户确认）+ 残留窗口被模糊搜索误判（真机调试：主窗口已正常关闭） | 已澄清，测试改用具体 hwnd 验证 |

### 修复后验证

- D3：`文件→退出`（U+2192）可直接使用 ✅
- D5：`pickElementUia → clickElementUia → inputElementUia` 全链通过（记事本 Edit）✅
- `test_desktop_commands.py` **9/9 通过**；全量 pytest **156 通过**；`harness:check` PASSED

### 未覆盖（D5 阻塞 / 依赖真机捕获）

- pickFromPathWin32 / pickElementUia→clickElementUia / inputElementUia：子层级解析被 D5 阻塞
- findParentWin32 / findSiblingWin32：依赖子控件链路，同样受 D5 影响

> 全量 pytest 147 → **155 通过 + 1 xfail**；`harness:check` PASSED。
