# 指令回归测试结果（三类既有方案）

日期：2026-08-14
执行：command-test skill（run_workflow.py，dev 后端 :8000，Edge 扩展在线）
方案依据：变量及日志 / 循环（20260810）/ 浏览器操作 既有测试方案

## 总览

| 方案 | 工作流 | 步骤 | 结果 | 耗时 |
|---|---|---|---|---|
| 浏览器操作 | wf=10 浏览器操作测试 | 10 | **10/10，0 失败** | 15.9s |
| 变量及日志 | wf=22 变量及日志测试 | 20 | **20/20，0 失败** | 4.4s |
| 循环 W1 纯循环 | wf=24 循环指令测试-W1 | 50(33节点) | **success，0 失败**（completed=39/50，break/continue 不计数为已知行为） | 0.8s |
| 循环 W2 条件中断 | wf=25 循环指令测试-W2 | 13 | **success，0 失败**（completed=9/13） | 0.7s |
| 循环 W3 浏览器循环 | wf=27 循环指令测试-W3 | 25 | **25/25，0 失败** | 19.0s |

## 分项确认

### 浏览器操作（wf=10，真实站点 baidu/bing/example/bilibili）

launchBrowser(edge) → newTab(bing) → switchTab(baidu) → navigate(example.com) →
newTab(bilibili) → switchTab(example) → closeTab(example) → switchTab(bilibili) →
navigate(baidu) → closeBrowser —— 全链路窗口/标签切换与 URL 断言正确 ✅

### 变量及日志（wf=22）

- setVar 全值类型：文本/数字/布尔/列表/字典/表达式(=len(list1)+1→4)/自动(JSON 推断)/引用({{text1}})/串内插值("42件") ✅
- log 三级别：info/warning/error；`{{undef}}` 未定义变量原样保留（P3 已知行为）✅
- 浏览器段：launchBrowser → getText("百度一下") → getElementLink(https://www.baidu.com/my/index) → navigate → closeBrowser ✅

### 循环（W1/W2/W3）

- W1：forRange 1-4 / 负步长 10→2 / forList 城市列表 / **listName 兼容（S4 0:a,1:b）** / while 表达式 n=1,2,3 / while varEquals once / 熔断 3 tick ✅
- W2：break/continue 条件中断（B1-B3 已修复回归通过）✅
- W3：forEachElement 遍历百度搜索结果 0-8 项；部分项 `contextNotFound` 软结果（元素在当前循环项中未找到 → onError=continue → 空值+stepWarning）为**已知行为**（SKILL P6），非失败 ✅

## 结论

三类既有方案全部回归通过，0 失败；结合本轮 T1/T2/T3（56 用例 + 浏览器 E2E + 桌面真机），
53 指令全量测试闭环完成。全量 pytest 156 通过，`harness:check` PASSED。
