{
  "cmd": "electronClick",
  "label": "点击元素",
  "runtime": "backend",
  "icon": "fa-hand-pointer",
  "iconColor": "text-indigo-500",
  "bgColor": "bg-indigo-50",
  "categoryOrder": 55,
  "commandOrder": 3,
  "description": "点击 Electron 应用页面中的元素（CSS/XPath/文本选择器，或元素库 electron 元素）",
  "enabled": true,
  "isNew": true,
  "params": [
    {"name": "elementName", "label": "元素库元素（可选）", "type": "string", "default": "",
     "placeholder": "已捕获的 electron 元素名称；提供后优先使用其选择器"},
    {"name": "selector", "label": "选择器（可选）", "type": "string", "default": "",
     "placeholder": "CSS 或 xpath: 或 text:；未填元素库元素时必填"},
    {"name": "titleFragment", "label": "页面标题筛选（可选）", "type": "string", "default": ""}
  ],
  "handler": {"kind": "backend", "source": "src/runtime/commands/electron_commands/electronClick.py"},
  "categories": ["electron"],
  "category": "Electron 应用"
}
